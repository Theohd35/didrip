# DidVIP TV

A focused Android TV WebView shell for DidVIP, designed for D-pad-only navigation and fullscreen media playback.

## Build

Use JDK 17 and the Android SDK (API 35):

```bash
gradle assembleDebug
```

Install `app/build/outputs/apk/debug/app-debug.apk` on the Formuler Z11 Pro Max.
